module.exports = {
  plugins: [
    require("tailwindcss"), // This line ensures Tailwind CSS is used as a PostCSS plugin
    require("autoprefixer"), // Optional, but typically recommended for vendor prefixes
  ],
};
